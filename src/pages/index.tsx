import { Layout } from "@/components/layout";
import HeroSection from "@/components/HeroSection";
const Home = () => {
  return (
    <Layout>
      <HeroSection />
    </Layout>
  );
};
export default Home;
